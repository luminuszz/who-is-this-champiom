import { DefaultSeoProps } from 'next-seo';

const SEO: DefaultSeoProps = {
  description: 'Procure seus campeões favoritos do lol',
  title: 'Who is this champiom ? | Procure seus campeões favoritos',
  titleTemplate: 'Who is this champiom',
};

export default SEO;
